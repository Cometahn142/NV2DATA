# NV2DATA API

## `NV2DATA.GetStore(config, template)`
Initializes a new data store.
- `config`: Table containing `DataStoreName`.

## `Store:LoadProfileAsync(key, force)`
Loads a player profile safely using session locking.

## `Profile:Reconcile()`
Merges default template values into existing data.
